<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyJourney.MainPage">


    <StackLayout VerticalOptions="Start" BackgroundColor="White"
                 CompressedLayout.IsHeadless="True"
                  >
        <!--<Image Source="plane.png" />-->

        <Image x:Name="iconimage" Aspect="AspectFit" HorizontalOptions="FillAndExpand" Margin="0,0,0,50"
              />
        
        <Entry x:Name="emailentry" Placeholder="Email Address" 
               Text="{Binding Users.Email,Mode=TwoWay}"
               Keyboard="Email"/>

        <Entry x:Name= "passwordentry" Placeholder="Password" IsPassword="True"
               Text="{Binding Users.Password,Mode=TwoWay}"/>


        <Button x:Name="LoginButton" Text="Log in"  Command="{Binding LoginCommand}"
                CommandParameter="{Binding Users}"
                BackgroundColor="DodgerBlue" TextColor="AntiqueWhite"
                Margin="40"/>
        
        <Button x:Name="registerUserButton" HorizontalOptions="End" Command="{Binding RegisterNavigationCommand}"   Text="Register"/>



    </StackLayout>

</ContentPage>
