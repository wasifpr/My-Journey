<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MyJourney.RegisterPage">
    <ContentPage.Content>
        <StackLayout x:Name="containerStackLayout"  VerticalOptions="FillAndExpand"   
                  >
            <!--<Image Source="plane.png" />-->

           

            <Entry x:Name="emailentry" Text="{Binding Email, Mode=TwoWay}" Placeholder="Email Address" 
               Keyboard="Email"/>

            <Entry x:Name= "passwordentry" 
                   Text="{Binding Password,Mode=TwoWay}"
                   Placeholder="Password" IsPassword="True"/>
            
            <Entry x:Name= "ConfirmPasswordEntry"
                   Placeholder=" Confirm Password" IsPassword="True"
                   Text="{Binding ConfirmPassword,Mode=TwoWay}"
                   />

            <Button x:Name="registerButton" Text="Log in"  Command="{Binding RegisterCommand}"
                    CommandParameter="{Binding Users}"
                BackgroundColor="DodgerBlue" TextColor="AntiqueWhite"
               />


        </StackLayout>
    </ContentPage.Content>
</ContentPage>